# Claude Adapter Notes

Claude skills commonly use a self-contained skill folder with a required `SKILL.md` and optional bundled resources such as `scripts/`, `references/`, and `assets/`.

Keep the shared instructions in `SKILL.md`. Add Claude-specific notes only when the target runtime requires them.

